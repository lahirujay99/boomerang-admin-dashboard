# Server Configuration
server.port=8080

# Database Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/boomerang_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=abcd1234A@

# JPA (Java Persistence API) / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
# 'update' will automatically update the database schema based on your entities.
# Use 'create-drop' for testing (deletes everything on restart) or 'validate' for production.

spring.jpa.show-sql=true
# 'show-sql' is useful for debugging because it prints the generated SQL queries to the console.

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Specifies the correct SQL "flavor" for MySQL.